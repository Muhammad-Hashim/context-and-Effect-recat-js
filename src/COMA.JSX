import React, { createContext } from 'react';

const hashim = createContext

const coma = () => {


    return (
        <>
            <hashim.Provider value={"hashim"}> 
            
            </hashim.Provider>
        </>
    );

};


export default coma;
export {hashim};